# Example environment variables for sub-agents (do NOT commit real secrets)
# Copy to `.env` in this same directory and fill in your actual values.
# NOTE: `.env` is ignored by .gitignore.

ANTHROPIC_API_KEY=YOUR_ANTHROPIC_KEY_HERE
